import React from "react";
import "./Code.scss";

const Code = (props) => {
  return (
    <div className="Code">
      <span className="Codetxt">{props.sif}</span>
    </div>
  );
};

export default Code;
